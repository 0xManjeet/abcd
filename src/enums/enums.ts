export enum ResponseTypes {
    QuestionDoesNotExists,
    Success,
    UnexpectedError,
}

export enum ActionTypes {
    NewQuestion,
    ShowDialog,
    EndQuiz,
    SubmitAnswers
}

export enum AnswerTypes {
    MCQ,
    Phone,
    Date,
    Text,
    GenderSelection,
}

